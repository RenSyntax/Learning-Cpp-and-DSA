#include <bits/stdc++.h>
using namespace std;

void unordered_set() {
    // Its similar to set, but only Unique, not ordered.
    unorderedset <int> ust;

    // all operations are similar to set but lower bound and upper bound function does not work.
    // all operations are mostly in o(1) time complexity.
}